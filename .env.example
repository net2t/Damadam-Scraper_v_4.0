# ================================================================================
# .env.example - DamaDam Scraper Configuration Template
# ================================================================================
# INSTRUCTIONS:
#   1. Copy this file: cp .env.example .env
#   2. Fill in your actual values
#   3. Keep .env in .gitignore (never commit to GitHub)
#   4. For GitHub Actions, add values to Settings > Secrets
# ================================================================================

# ============ DAMADAM CREDENTIALS ============
# Your DamaDam.pk account login credentials

## Primary account (required)
DAMADAM_USERNAME=your_username
DAMADAM_PASSWORD=your_password

## Secondary account (optional - used as fallback if primary fails)
DAMADAM_USERNAME_2=
DAMADAM_PASSWORD_2=

# ============ GOOGLE SHEETS ============
# Your Google Sheet where data will be stored

## Google Sheet URL (must be shared with service account)
## Format: https://docs.google.com/spreadsheets/d/SHEET_ID/edit
GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/xxxxx/edit

## Path to Google Service Account credentials file
## File should be in root directory (never commit to GitHub)
## For GitHub Actions, use GOOGLE_CREDENTIALS_JSON instead
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

## Raw Google credentials JSON (for GitHub Actions only)
## Copy entire credentials.json content as raw JSON string
## Do NOT use this locally, use GOOGLE_APPLICATION_CREDENTIALS instead
GOOGLE_CREDENTIALS_JSON=

# ============ SCRAPING BEHAVIOR ============
# These control how the scraper behaves

## Maximum profiles to scrape per run (0 = unlimited)
MAX_PROFILES_PER_RUN=0

## Number of profiles to process before taking a pause
BATCH_SIZE=20

## Minimum delay between requests (seconds)
## Increase this if hitting rate limits
MIN_DELAY=0.3

## Maximum delay between requests (seconds)
## Increase this if hitting rate limits
MAX_DELAY=0.5

## Timeout for page loads (seconds)
PAGE_LOAD_TIMEOUT=30

## Delay when writing to Google Sheets (seconds)
## Increase if hitting API quota
SHEET_WRITE_DELAY=1.0

# ============ BROWSER (Optional) ============
# Usually auto-detected, only change if needed

## Path to ChromeDriver executable (local mode only)
## If empty, uses system ChromeDriver
CHROMEDRIVER_PATH=chromedriver.exe

# ================================================================================
# NOTES:
# ================================================================================
# 1. Local Mode:
#    - Copy to .env
#    - Put credentials.json in root
#    - Run: python main.py
#
# 2. GitHub Actions:
#    - Add values to Settings > Secrets
#    - Use GOOGLE_CREDENTIALS_JSON for credentials (not file)
#    - Workflow triggered automatically or manually
#
# 3. Security:
#    - NEVER commit .env file
#    - NEVER commit credentials.json
#    - Use strong passwords
#    - Use separate accounts for testing
#
# 4. Troubleshooting:
#    - If LOGIN_FAILED: Check DAMADAM_USERNAME/PASSWORD
#    - If SHEETS_ERROR: Check GOOGLE_SHEET_URL
#    - If AUTH_ERROR: Check Google credentials file
# ================================================================================
